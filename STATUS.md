# Текущий статус Hermes-версии

Обновлено: 18 сентября 2026 года.

## Состояние

- Ветка `feature/hermes-rebuild` создана как чистая orphan-ветка.
- Старый код проекта намеренно не перенесён.
- Подготовлены требования, архитектурные границы, карта страниц и брендовые assets.
- Реализация ещё не начата.

## Активная задача

`H0. Основа` из `ROADMAP.md`.

Первый законченный результат:

1. минимальное FastAPI-приложение;
2. `/api/health`;
3. корневая HTML-страница с подключёнными локальными CSS/JS;
4. конфигурация отдельной SQLite-БД;
5. requirements и Docker Compose;
6. pytest health check;
7. документированная команда запуска на `127.0.0.1:8011`.

## Проверки

Пока отсутствуют: код ещё не создан.

## Блокеры

Нет для H0–H2. Для наполнения последующих разделов нужны материалы из списка
`CONTENT.md`.

## Правило обновления

После каждой сессии Hermes заменяет этот статус фактическими данными: commit,
реализованные сценарии, команды и результаты проверок, решения, блокеры и один
следующий конкретный шаг.
